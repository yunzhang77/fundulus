### Installation Direction of FastQC
To download correct FastQC: 
sudo apt-get remove fastqc

wget https://launchpad.net/ubuntu/+archive/primary/+files/fastqc_0.11.5+dfsg-3_all.deb

sudo dpkg -i fastqc_0.11.5+dfsg-3_all.deb

sudo apt-get install -f

Bash file to run ALL .fastq files in fastqc (type nano fastqc.sh to make this bash file in terminal): 
#!/bin/bash

for d in Desktop/fastq/*.fastq; do
  fastqc $d
Done

NOTE: MUST CHANGE PATH (Desktop/fastq) to where you store your .fastq files from where your script is saved

Change permissions of your bash script:
chmod 777 fastqc.sh

Now run it!
./fastqc.sh
